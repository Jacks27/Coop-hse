language: 
    - python
python:
    - "3.6.7"
    - "3.5"
install:
    - pip install -r requirements.txt
    - pip install nose
@@ -13,4 +13,4 @@ script:
    - nosetests --with-coverage --cover-package=app
after_success:
    - coveralls
    - codecov